#include <iostream>
using namespace std;

int main() {
    int N;
    cin >> N;
    
    int count = 0;
    int temp = N;
    
    while (temp > 0) {
        count++;
        temp /= 10; // Remove the last digit
    }
    
    cout << count << endl;
    
    return 0;
}